// Shared enums so sections can import without circular deps.
enum RangePreset { d7, d30, m3, m4, m6, y1, ytd, custom }

enum Dimension { clients, services }

enum EventTypeFilter { all, simple, work } // work = work_service/work_visit
